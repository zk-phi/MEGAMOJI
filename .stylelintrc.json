{
    "extends": "stylelint-config-primer",
    "rules": {
        "plugin/no-unsupported-browser-features": [true, {
            "severity": "error",
            "ignore": ["css-gradients"]
        }]
    }
}
